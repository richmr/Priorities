// Various data grabbers and getters

function getPortfolios() {
	// Returns the list of portfolios for this user
	return 0;
}

function getPeople( portfolio ) {
	// Returns the current list of names being used in the portfolio
	return getPeople_test();
}

function getPriorityTableSettings( portfolio ) {
	// Will eventually return the number of rows in the priority table
	// the number of chips per column
	// and the name at the front of the row
	return 0;
}

function getProjectBasics( portfolio ) {
	// Returns the basic info on every project in the portfolio
	return getProjectBasics_test();
}

function getPriorityTableProjects( portfolio ) {
	// Not currently implemented, but will return the data 
	// currently stored in "priorityChips"
	return getPriorityTableProjects_test();
}

function getArchivedProjects ( portfolio ) {
	// Not currently implemented, but will return the data 
	// currently stored in "archivedProjects"
	return getArchivedProjects_test();
}

function getDoneProjects ( portfolio ) {
	// Not currently implemented, but will return the data 
	// currently stored in "doneProjects"
	return 0;
}

function getGraveyardProjects ( portfolio ) {
	// Not currently implemented, but will return the data 
	// currently stored in "graveyardProjects"
	return 0;
}

